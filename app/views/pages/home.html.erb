<% if user_signed_in? %>
  <div class="landing-dashboard container mt-4" data-controller="greeting" data-greeting-first-name-value="<%= @first_name %>">
    <div class="landing-dashboard__inner">
      <div class="text-center gap-32">
        <h1 class="page-title mb-2" data-greeting-target="greeting"></h1>
      </div>

      <div class="d-flex justify-content-center mb-4">
        <div class="landing-dashboard__date-nav app-card d-flex justify-content-between align-items-center">
          <%= link_to root_path(date: (@selected_date - 1.day).iso8601), class: "landing-dashboard__nav-link text-center text-decoration-none" do %>
            <p class="section-header mb-1">Yesterday</p>
            <i class="bi bi-arrow-left text-muted arrow-icon"></i>
          <% end %>

          <%= link_to root_path, class: "landing-dashboard__today-pill badge-primary-soft rounded-pill d-flex flex-column align-items-center justify-content-center text-center text-decoration-none" do %>
            <p class="section-header text-primary mb-1 today-label"><%= @selected_date.strftime("%A") %></p>
            <p class="text-muted mb-0 today-date"><%= @selected_date.strftime("%B %-d") %></p>
          <% end %>

          <%= link_to root_path(date: (@selected_date + 1.day).iso8601), class: "landing-dashboard__nav-link text-center text-decoration-none" do %>
            <p class="section-header mb-1">Tomorrow</p>
            <i class="bi bi-arrow-right text-muted arrow-icon"></i>
          <% end %>
        </div>
      </div>

      <div class="d-flex justify-content-center gap-40">
        <div class="landing-dashboard__progress-card app-card mx-auto">
          <% percentage = @total_today_count.zero? ? 0 : (@taken_today_count * 100.0 / @total_today_count).round %>
          <% remaining = @total_today_count - @taken_today_count %>
          <div class="d-flex justify-content-between align-items-center mb-2">
            <p class="mb-0 progress-label">Today's progress</p>
            <p class="text-primary mb-0 progress-count"><%= @taken_today_count %> / <%= @total_today_count %></p>
          </div>
          <div class="progress">
            <div class="progress-bar" role="progressbar" style="--progress-width: <%= percentage %>%;" aria-valuenow="<%= percentage %>" aria-valuemin="0" aria-valuemax="100"></div>
          </div>
          <div class="d-flex justify-content-between align-items-center mt-2">
            <p class="mb-0 progress-message">
              <%= remaining <= 0 ? "All done today 🎉" : "Keep going, #{@first_name} 🌱" %>
            </p>
            <p class="text-primary mb-0 progress-percent"><%= percentage %>%</p>
          </div>
        </div>
      </div>

      <% day_word = if @selected_date == Date.current
                      "Today"
                    elsif @selected_date == Date.current - 1.day
                      "Yesterday"
                    elsif @selected_date == Date.current + 1.day
                      "Tomorrow"
                    else
                      @selected_date.strftime("%A")
                    end %>
      <div class="text-center">
        <h2 class="text-primary mb-3 landing-dashboard__section-title"><%= day_word %>'s medications 📅</h2>
        <p class="text-muted fst-italic mb-4 landing-dashboard__section-subtitle">Scheduled doses for <%= @selected_date.strftime("%B %-d") %></p>
      </div>

      <% if @today_medications.empty? %>
        <p class="text-muted text-center">No medications scheduled.</p>
      <% else %>
        <div class="horizontal-scroll-wrapper" data-controller="medication-modal">
          <% @today_medications.each do |medication| %>
            <%= render "medications/medication_summary_card", medication: medication, date: @selected_date %>
          <% end %>
          <%= render "medications/medication_modal", medications: @today_medications %>
        </div>
      <% end %>
    </div>
  </div>
<% else %>
  <section class="hero-section text-center text-white d-flex align-items-center">
    <div class="container">
      <h1 class="display-4 fw-bold mb-3">Never miss a dose again</h1>
      <p class="lead mb-4">
        MediLink helps you track your medications and stay connected with your family,
        so everyone gets the care and reminders they need, all in one place.
      </p>
      <div class="d-flex justify-content-center gap-3">
        <%= link_to "Sign up", new_user_registration_path, class: "btn btn-light btn-lg" %>
        <%= link_to "Log in", new_user_session_path, class: "btn btn-outline-light btn-lg" %>
      </div>
    </div>
  </section>

  <section class="how-it-works py-5 section-gap">
    <div class="container">
      <h2 class="text-center mb-5">How it works</h2>
      <div class="row text-center g-4">
        <div class="col-md-4">
          <i class="bi bi-capsule how-it-works-icon"></i>
          <h3 class="h5 mt-3">Track your medications</h3>
          <p class="text-muted">Keep a clear record of every medication, dosage and schedule for you and your family.</p>
        </div>
        <div class="col-md-4">
          <i class="bi bi-people-fill how-it-works-icon"></i>
          <h3 class="h5 mt-3">Stay connected with family</h3>
          <p class="text-muted">Add family members and follow their treatments from a single shared account.</p>
        </div>
        <div class="col-md-4">
          <i class="bi bi-bell-fill how-it-works-icon"></i>
          <h3 class="h5 mt-3">Never miss a reminder</h3>
          <p class="text-muted">Get timely reminders so doses are never forgotten, for yourself or your loved ones.</p>
        </div>
      </div>
    </div>
  </section>

  <footer class="footer text-center py-4">
    <div class="container">
      <p class="mb-0">&copy; <%= Date.current.year %> MediLink</p>
    </div>
  </footer>
<% end %>
